# 室内小车导航重构项目阶段性技术汇报

日期：2026-09-13

## 1. 项目目标

本项目基于原比赛工程中已经过实车验证的算法，重构一套不包含机械臂任务的室内小车导航系统，主要目标包括：

- 使用 Livox 激光雷达和 FAST-LIO 提供实时点云与里程计。
- 基于固定二维地图和 AMCL 完成全局定位。
- 沿预先生成的闭环轨迹进行自主跟踪。
- 使用局部 Hybrid A* 处理局部障碍和路径重规划。
- 通过 Safety 节点统一执行定位、底盘、障碍物和跟踪误差保护。
- 保证真实底盘只接收经过 Safety 审核的速度指令。

本阶段的主要验收目标，是在有人全程监督的条件下，让小车以低速完整跑通约 155 m 的室内闭环路线。

## 2. 当前阶段成果

截至 2026-09-13，已经完成以下工作：

- 建立独立重构工作区 `/home/agilex/competition_rebuild_ws`，原比赛工作区保持不修改。
- 打通 Livox、FAST-LIO、二维地图、AMCL、MPPI、局部 Hybrid A*、Safety、Ranger 底盘完整链路。
- 完成 0.8 m 直行、动态障碍停车与恢复、3 m 静态障碍绕行、10 m 和 30 m 分段测试。
- 查明转弯长时间停顿和车轮突然回正的主要原因，并完成参数与轨迹修正。
- 使用最终基线完成约 155 m、3109 个轨迹点的整圈实车测试。
- 整圈测试最高速度为 `0.20 m/s`，四个主要转弯均通过。

当前有效的运动链路为：

```text
离线全局轨迹
-> MPPI 跟踪控制
-> Safety 状态与误差保护
-> Ranger twist adapter
-> /cmd_vel
-> Ranger 底盘
```

需要强调：整圈跑通证明当前组合形成了一个可工作的实车基线，但不代表建图、定位、局部规划和高速度运行已经全部工程化闭环。

## 3. 最终通过的基线方案

### 3.1 运行环境

所有重构节点统一使用：

```bash
source /home/agilex/competition_rebuild_ws/scripts/source_rebuild_runtime.sh
```

`competition_control`、`competition_safety`、`competition_planning` 和 `rebuild_bringup` 必须来自重构工作区；`ranger_base` 来自 `/home/agilex/agilex_ws`。

### 3.2 地图与定位

- 固定地图：`maps/indoor_manual_final/map.yaml`
- 定位方式：FAST-LIO 实时点云投影为二维激光，再由 AMCL 匹配固定 PGM 地图。
- 固定起点：约 `x=-16.083 m`、`y=2.309 m`、`yaw=172.9 deg`。
- 每次启动后仍需发布初始位姿，并确认 `map -> body` 连续稳定、实时激光与地图边界贴合。

### 3.3 路径与速度

- 转弯设计半径：约 `1.20 m`。
- 整圈轨迹：`artifacts/indoor_manual_six_anchor_v3_radius120_turnrate250_020mps_trajectory.yaml`
- 轨迹点数：3109。
- 路线长度：约 155 m。
- 最高速度：`0.20 m/s`。
- 轨迹和 MPPI 的曲率变化率统一为 `2.50 1/(m*s)`。

### 3.4 Safety 恢复参数

最终采用接近原比赛验证值的跟踪误差带：

```text
进入恢复：横向误差 0.20 m，航向误差 20 deg
退出恢复：横向误差 0.10 m，航向误差 8 deg
硬限制：  横向误差 0.40 m，航向误差 45 deg
恢复速度：0.15 m/s
持续超时：1.0 s
```

近距障碍停车、数据时效检查、底盘状态检查和控制模式检查仍然保留。

## 4. 主要问题、分析与处理结果

### 4.1 建图漂移与地图重影

**现象**

FAST-LIO 建图过程中出现累计漂移，长走廊、重复结构和闭环区域存在墙体错位、重影、局部变厚及伪障碍。直接从原始点云生成的二维占据地图不能稳定作为导航地图。

**当前处理**

本阶段没有从算法根因上解决漂移，而是采用以下临时工程方案：

1. 保存 FAST-LIO 输出的 PCD 点云。
2. 使用 `scripts/pcd_to_pgm.py` 按高度范围筛选点云并投影为 PGM 占据栅格。
3. 人工擦除重影、漂移墙线、动态物体残影和明显伪障碍。
4. 将人工清理后的 PGM 作为静态地图供 AMCL 使用。

**状态：暂时绕过，尚未解决。**

该方案使当前固定路线能够运行，但地图质量依赖人工判断，缺乏可重复性，也可能误删真实障碍或误把未验证区域标为空闲。整圈通过不能反向证明原始建图无漂移。

**后续需要重点解决**

- 检查 Livox 与 IMU 外参、时间同步和运动畸变补偿。
- 分析长走廊退化、FAST-LIO 参数和点云匹配约束。
- 引入可靠闭环检测或全局图优化，减少累计误差。
- 建立原始 PCD、自动投影结果、人工修改记录和最终地图之间的版本追踪。
- 设计可量化的地图验收指标，例如闭环端点误差、平行墙间距误差和重复采集一致性。

### 4.2 ROS 工作区覆盖关系错误

**现象**

虽然已经编译重构工作区，但 `ros2 pkg prefix` 一度显示控制、规划和安全包仍来自原比赛工作区。修改后的代码没有真正被运行，导致现场现象和预期参数不一致。

**原因**

ROS underlay/overlay 的加载顺序混乱，同一终端重复 source 多个工作区，旧包覆盖了重构包。

**解决**

- 创建统一的 `source_rebuild_runtime.sh`。
- 在干净 shell 中按固定顺序加载 ROS、底盘工作区和重构 overlay。
- 使用 `ros2 pkg prefix` 检查实际生效路径。
- 重新编译重构工作区中的 7 个包。

**状态：已解决，运行流程已固化。**

### 4.3 重复 AMCL 和点云投影节点

**现象**

ROS 图中同时出现两个 `/amcl` 和两个 `/rebuild_localization_scan`，生命周期命令返回矛盾状态，点云投影节点还出现 QoS 不兼容警告。

**原因**

多次启动后旧进程未完全退出，同时保留了 `ros2 run` 包装进程及其子进程。

**解决**

- 同时清理包装进程和真实节点进程。
- 每类节点只允许保留一个实例。
- 启动 AMCL 前先检查节点列表和进程列表。
- 仅根据当前 lifecycle 状态执行合法转换，已经 active 时不再重复 configure/activate。

**状态：已解决，但仍依赖规范启动顺序。**

### 4.4 自动控制有速度指令但底盘不运动

**现象**

`/cmd_vel_safe` 与 `/cmd_vel` 均有非零速度，Ranger 驱动器状态为使能，但 `/odom`、电机 RPM 和 CAN TX 计数保持不变；遥控模式下电机可以正常运动。

**分析结果**

- `driver_state: 64` 是驱动使能位，不是故障码。
- 底盘硬件、CAN 回传和电机本体基本正常。
- `ugv_sdk` 的异步 CAN 发送存在缓冲区生命周期风险。
- `ranger_mini_v3` 参数分支还存在控制流问题。

**解决**

- 修正 CAN 发送实现，使用有效生命周期内的发送方式。
- 修正 Ranger Mini V3 参数分支。
- 重新编译 `ugv_sdk` 和 `ranger_base`。
- 用 CAN TX 计数、`/cmd_vel`、`/odom` 和电机 RPM 联合验证。

**状态：已解决并经实车验证。**

### 4.5 路径来源清单不匹配导致 MPPI 退出

**现象**

部分新轨迹加载时，MPPI 报告 `trajectory source mismatch`，包括 route、semantic map、planning params 和 optimizer params 的 SHA256 不匹配。

**原因**

轨迹文件包含来源清单，但通用 launch 仍传入旧路线和旧配置，控制器按设计拒绝加载不一致的轨迹。

**解决**

- 为 R=0.9 m、R=1.2 m 和不同曲率参数建立独立配置与 launch。
- 重新生成轨迹来源清单。
- 启动前确认 MPPI 输出正确轨迹路径和点数。

**状态：已解决。来源校验机制应继续保留。**

### 4.6 原始转弯半径过小

**现象**

原始闭环在四个转弯处出现明显减速、转向不足和等待。

**分析结果**

- 原始轨迹最小转弯半径约 `0.62 m`，最大曲率约 `1.613 1/m`。
- 虽然底盘理论上能够进行较急转弯，但轨迹、实际执行误差和 Safety 条件叠加后稳定裕度不足。
- R=0.9 m 版本降低了曲率，但仍未消除长时间停车。

**解决**

- 重新构造四个转弯的语义锚点。
- 将有效转弯半径扩大到约 `1.20 m`。
- 重新生成 30 m 和整圈轨迹。

**状态：几何问题已改善并通过整圈验证，但曲率连接仍不是严格连续曲线。**

### 4.7 轨迹与控制器曲率变化率不一致

**现象**

轨迹已经按较高曲率变化率重新定时，但实车 MPPI 仍使用较低限制，导致入弯时无法按轨迹预期及时建立转角。

**原因**

轨迹生成参数为 `2.50 1/(m*s)`，运行中的 MPPI 曾保持 `0.90 1/(m*s)`。

**解决**

- 创建同步后的独立 launch。
- 将轨迹参数化器和 MPPI 运行参数统一为 `2.50 1/(m*s)`。
- 启动后使用 `ros2 param get` 验证实际参数。

**状态：已解决。**

### 4.8 转弯长时间停车和车轮突然回正

**现象**

小车能够进入转弯，但行驶一段后车轮突然回正并停止，有时持续数分钟。动态障碍物靠近时偶尔又出现小幅动作，最初容易误判为障碍检测或逐点跟踪问题。

**rosbag 证据**

- MPPI 在入弯阶段持续产生正确的转向命令。
- `/cmd_vel_safe`、`/cmd_vel` 和 `/odom` 在停车前能够一致执行转弯。
- 局部规划状态多次为 `REFERENCE_CLEAR`，停车并非由固定障碍首先触发。
- Safety 在横向误差约 `0.12-0.17 m` 时过早进入低速恢复。
- 恢复速度被压到约 `0.05 m/s`，随后触发 `lateral_error_persistent`。
- Safety 输出归零后，底盘按零角速度命令自动回正车轮。
- 后续 Hybrid A* 无解是车辆已经偏离后的结果，不是最初原因。

**根因**

重构版 Safety 的跟踪恢复带比原比赛参数过窄。正常转弯误差被当作持续异常处理，形成“减速、转向能力下降、误差恢复变慢、超时停车”的闭环。

**解决**

- 恢复接近比赛实车验证过的 Safety 跟踪误差带。
- 将恢复速度由 `0.05 m/s` 提高到 `0.15 m/s`。
- 保留硬误差限制、近距障碍停车和状态时效检查。
- 配合 R=1.2 m 路径和统一曲率变化率进行 30 m 复测。

**状态：主要故障已解决。30 m 和整圈实车均通过。**

### 4.9 局部规划瞬时停车与无可行路径

**现象**

`/planning/local_stop_request` 偶尔瞬时变为 true，局部规划器出现 `HYBRID_ASTAR_NO_FEASIBLE_PATH`、`HYBRID_ASTAR_TIMEOUT` 或数据过期状态。有一次监督脚本因此拒绝发车，但人工稍后查看所有停车话题均为 false。

**处理**

- 增加局部短距离重接目标，依次尝试完整目标、约 2 m 和约 1.2 m 的重接点。
- 调整局部规划超时、空历史超时、数据时效和目标航向容差。
- 修正一次 `relaxed_extension_timeout_s` 大于总规划超时而导致节点启动失败的配置错误。
- 放弃会被瞬时历史值误触发的额外发车监督器，保留控制器和 Safety 原生保护。

**状态：整圈基线已通过，但局部规划的瞬时失败机制仍需专项压力测试。**

### 4.10 初始位姿与 TF 启动竞态

**现象**

`tf2_echo map body` 启动前几秒经常提示 frame 不存在，旧文档中的初始姿态四元数也与最终路线方向不一致。

**解决**

- 在 AMCL 已 active 后再发布初始位姿。
- 将起点姿态修正为 `z=0.998045, w=0.062506`。
- 不把最初几秒的 TF 等待信息直接判定为故障，只要随后能持续输出稳定变换即可。
- 发车前同时核对位置、航向和激光贴图情况。

**状态：已形成可用流程，但仍需要人工发布和确认。**

### 4.11 SSH 断开导致运行中止

**现象**

第一次整圈测试中 SSH 连接断开，前台 ROS launch 随终端退出，小车停止。

**处理原则**

- 导航进程继续保持前台运行，不使用无人监管的后台驻留方式。
- SSH 中断后由命令超时和 Safety 失联保护停车。
- 重新连接后检查没有残留导航节点，并从传感器、地图、定位到导航完整重启。
- 不从中断位置直接恢复整圈进度。

**状态：失联停车行为符合当前安全要求，但远程运行可靠性仍可改进。**

## 5. 测试结果汇总

| 测试项目 | 结果 | 主要验证内容 |
|---|---|---|
| 0.8 m 直行 | 通过 | 基本控制链、启动与停止 |
| 0.8 m 动态障碍 | 通过 | 障碍停车、移开后恢复 |
| 3 m 静态障碍 | 通过 | 局部 Hybrid A* 绕行 |
| 10 m 路线 | 通过 | 连续低速跟踪 |
| 旧几何 30 m | 未通过 | 第一转弯长时间停车 |
| R=0.9 m 30 m | 未通过 | 路径改善但 Safety 仍触发停车 |
| R=1.2 m 30 m | 通过 | 第一转弯连续完成，无动态干预 |
| R=1.2 m 整圈约 155 m | 通过 | 3109 点、0.20 m/s、四个转弯 |

本阶段的整圈测试主要验证固定路线的连续运行。它不能替代不同障碍布局、定位退化、传感器丢帧、网络中断和多次重复运行测试。

## 6. 尚未解决的关键问题

### 6.1 建图漂移仍是最高优先级问题

当前最终地图来自 PCD 投影和人工擦除，不是建图算法稳定闭环的结果。若场地变化、重新采图或扩大路线，人工地图流程可能再次成为主要风险。

### 6.2 地图制作流程缺乏自动化和可追溯性

目前没有自动识别重影、动态点和漂移墙线的可靠方法，也没有完整记录每一次人工擦除的区域与理由。

### 6.3 局部规划鲁棒性尚未充分验证

整圈无障碍通过不等于复杂障碍场景稳定。Hybrid A* 的瞬时无解、超时、数据时效和路径重接策略仍需独立测试。

### 6.4 定位仍依赖固定起点和人工初始位姿

当前流程依赖小车放回固定起点、人工发布初始位姿并在 RViz 中确认。任意位置重定位和自动初始化尚未实现。

### 6.5 转弯路径仍有曲率不连续

R=1.2 m 路径已通过实车，但直线与圆弧连接处仍存在离散曲率跳变。当前参数能够容忍该变化，尚未实现严格的 clothoid 或其他连续曲率过渡。

### 6.6 更高速度尚未验证

当前可靠基线为 `0.20 m/s`。直接提高到 `0.50 m/s` 会显著增加转向动态、横向加速度、制动距离和局部规划响应要求，因此不能用本次低速整圈结果直接推断高速可靠性。

## 7. 下一阶段建议

建议按以下优先级推进：

1. 冻结当前 R=1.2 m、0.20 m/s 整圈基线和全部配置，不再直接覆盖。
2. 对建图漂移开展专项测试，优先检查时间同步、外参、退化场景和闭环优化。
3. 固化 PCD 到 PGM 的可重复流水线，保存原始地图、自动结果、人工修改版本和差异图。
4. 对局部规划器开展静态障碍、动态障碍、狭窄通道和连续重规划压力测试。
5. 增加启动前自动检查，但避免读取瞬时历史值后永久拒绝发车。
6. 增加多圈重复测试，统计定位漂移、最大横向误差、Safety 状态切换和局部规划失败次数。
7. 在不修改 0.20 m/s 基线的前提下另建 0.30 m/s 候选，先完成 30 m 转弯测试，再考虑整圈。
8. 只有 0.30 m/s 重复通过且制动与障碍测试满足要求后，再评估 0.50 m/s。

## 8. 阶段结论

本阶段已经解决了运行环境覆盖、CAN 自动发送、轨迹来源校验、转弯半径、曲率变化率不一致以及 Safety 恢复阈值过严等关键问题，并最终完成约 155 m 的低速整圈实车运行。

其中，转弯停车的直接原因已经通过 rosbag 证据定位并修复，不是小车必须严格经过每一个离散轨迹点，也不是底盘无法执行连续转向。

但建图漂移尚未从根本上解决。当前地图依赖 PCD 转 PGM 后的人工擦除，属于可用的阶段性工程绕行方案。项目现状应准确表述为：固定场地、人工清理地图、固定起点和低速条件下已经跑通；自动建图质量、自动重定位、复杂局部规划和更高速度仍需继续完善。
